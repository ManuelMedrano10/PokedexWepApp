<div class="row d-flex justify-content-center">
    <div class="col-12 col-sm-4">
        <div class="card">
            <div class="card-header bg-dark">
                <h5 class="text-light text-center"><i class="bi bi-tv"></i>{{#if editMode}} Edit type {{type.name}}
                    {{else}} New type {{/if}}</h5>
            </div>
            <div class="card-body">
                <form action="/types/{{#if editMode}}edit{{else}}create{{/if}}" method="post">
                    <div class="mb-3">
                        <label for="name" class="form-label">Name: <span class="text-danger">*</span></label>
                        <input type="text" value="{{#if editMode}}{{type.name}}{{/if}}" class="form-control"
                            name="name" id="name" required="required">
                    </div>

                    {{#if editMode}}
                    <input type="hidden" name="typeId" value="{{type.id}}">
                    {{/if}}

                    <div class="mb-3">
                        <button type="submit" class="btn btn-secondary float-end"><i class="bi bi-bookmark"></i>
                            Save</button>
                        <a href="/types/index" class="btn btn-outline-warning float-end me-2"><i
                                class="bi bi-door-open"></i> Back</a>
                    </div>
                </form>
            </div>
        </div>
    </div>
</div>